# Cursor Rules
# 位置: ~/.cursorrules (全局) 或项目根目录 .cursorrules
# 用途: Cursor IDE 的 AI 助手行为规范

# =============================================================================
# 核心工作流：三阶段强制确认制
# =============================================================================

# 铁律：禁止直接编写实现代码
# 在任何情况下，必须遵循以下三阶段工作流

---

## Phase 1: 需求澄清与确认 (REQUIREMENT)

### 首次响应模板

当用户提出任务时，你的第一句话必须是：

```
收到任务。在开始任何实现之前，我需要先完成需求澄清。

根据任务描述，我初步理解的需求如下：

【用户原始需求摘要】
- 目标: [一句话概括]
- 范围: [明确包含/不包含的内容]
- 约束: [已知的限制条件]

【待澄清问题】
我有以下问题需要确认：

1. [具体问题1] - 影响[具体方面]
2. [具体问题2] - 影响[具体方面]
...

请回答以上问题，或补充任何我遗漏的重要信息。
```

### 需求澄清检查清单

必须确认以下维度（如适用）：

- 功能范围: 必须实现什么？明确排除什么？
- 用户场景: 谁使用？在什么场景下使用？
- 输入/输出: 数据从哪来？输出到哪去？格式是什么？
- 性能要求: 吞吐量？延迟？并发量？
- 可靠性: 可用性目标？故障恢复要求？
- 安全: 认证？授权？数据保护？
- 兼容性: 需要支持哪些版本/平台？
- 约束条件: 技术栈限制？第三方依赖？预算？

### 需求确认标准

只有在以下情况下，才能结束 Phase 1：
- 所有待澄清问题已得到明确回答
- 用户确认"需求理解无误，可以进入设计阶段"
- 已识别并记录所有风险点和假设

---

## Phase 2: 设计与规划 (DESIGN)

### 设计产出物清单

用户确认需求后，必须在 `docs/` 目录产出以下文档：

#### 2.1 PRD-SPEC.md (需求规格说明书)

必须包含：
- 概述: 背景、目标、成功标准（可度量）
- 功能需求: 用户故事、功能列表（带优先级和验收标准）
- 非功能需求: 性能、安全、可用性指标
- 约束与假设: 技术/业务约束、关键假设及影响
- 风险分析: 风险矩阵（可能性×影响）
- 术语表: 统一语言

#### 2.2 ARCHITECTURE.md (架构设计说明书)

必须包含：
- 架构概述: 架构风格、技术栈及选型理由
- 系统架构图: 使用 Mermaid 或 ASCII 绘制
- 组件设计: 组件清单（单一职责）、组件交互图
- 数据设计: 数据模型（ER图/实体定义）、数据流
- 接口设计: 内部接口定义、外部API规范
- 质量属性设计:
  - 可扩展性: 水平/垂直扩展方案
  - 可靠性: 故障检测、恢复、降级策略
  - 可观测性: 日志、指标、追踪规范
  - 安全性: 认证、授权、数据保护
- 决策记录 (ADR): 关键决策的权衡分析

#### 2.3 TEST_STRATEGY.md (测试策略)

必须包含：
- 测试分层: 单元/集成/E2E 的范围、工具、覆盖率目标
- 单元测试规范: 命名约定、必测场景、Mock 策略
- 集成测试规范: 组件契约验证、测试数据管理
- 测试数据: 工厂模式、禁止共享可变状态

### 设计确认流程

产出设计文档后，必须：

```
设计文档已完成，位于 docs/ 目录：
- PRD-SPEC.md: 需求规格
- ARCHITECTURE.md: 架构设计
- TEST_STRATEGY.md: 测试策略

请审阅以上文档，确认：
1. 需求理解是否正确？
2. 架构设计是否满足要求？
3. 是否有遗漏或需要调整的地方？

确认无误后，我将进入实现阶段。
```

只有在用户明确回复"设计确认无误，可以开始实现"后，才能进入 Phase 3。

---

## Phase 3: 实现 (IMPLEMENTATION)

### 3.1 实现前准备

确认设计后，首先创建 `docs/IMPLEMENTATION_PLAN.md`：

- 任务分解: 具体任务、依赖关系、时间估算、可验证产出
- 实现顺序: 说明依赖关系和优先级
- 验证清单: 每个任务可独立验证

### 3.2 编码规范

根据项目语言，严格遵循对应规范（见下方语言特定规范）。

### 3.3 增量交付

- 每个功能点完成后，主动展示代码并请求反馈
- 禁止一次性提交大量代码
- 遇到设计未覆盖的情况，暂停并回到 Phase 2

---

# 语言特定规范

## Java

- 遵循 Google Java Style Guide
- 使用 final 修饰不可变变量和参数
- 优先使用 Optional 而非 null
- 异常处理: 受检异常用于可恢复错误，运行时异常用于编程错误
- 依赖注入: 使用 Spring/Guice 等框架
- 测试: JUnit 5 + AssertJ + Mockito

## Python

- 遵循 PEP 8 + Google Python Style Guide
- 类型注解: 所有函数必须有类型提示
- 使用 dataclasses 或 Pydantic 定义数据结构
- 异常: 自定义异常继承自特定基类
- 依赖管理: 使用 pyproject.toml + poetry/uv
- 测试: pytest + pytest-cov

## TypeScript

- 遵循 Google TypeScript Style Guide
- 严格模式: strict: true
- 禁止 any，使用 unknown + 类型守卫
- 优先使用 interface 定义对象形状
- 错误处理: 使用 Result/Either 模式或明确抛出
- 测试: Jest/Vitest

## Go

- 遵循 Effective Go + Uber Go Style Guide
- 错误处理: 显式检查，错误信息小写开头
- 接口: 小接口优于大接口
- 并发: 使用 context 传递取消信号
- 测试: 表格驱动测试，t.Parallel()

---

# 通用代码规范

## 命名

- 变量/函数: 清晰表达意图，避免缩写（除非行业标准）
- 类/模块: 名词，单一职责
- 布尔值: 使用 is/has/should/can 前缀

## 函数设计

- 单一职责: 一个函数只做一件事
- 参数数量: ≤3 个，超过使用配置对象/结构体
- 返回值: 错误作为最后一个返回值（Go风格）或用异常/Result
- 副作用: 在函数名中明示（如 `saveXxx`, `mutateXxx`）

## 注释

- 为什么 > 做什么 > 怎么做
- 公共 API 必须有文档注释
- 复杂算法必须解释原理

## 错误处理

- 错误信息必须包含上下文（哪个操作、哪个实体）
- 区分用户错误（4xx）和系统错误（5xx）
- 日志记录: 错误发生点记录，传播时包装

---

# 禁止事项

- ❌ 直接开始写代码而不经过需求确认
- ❌ 设计未确认就进入实现
- ❌ 一次提交超过 200 行变更（不含测试）
- ❌ 使用 "TODO" 或 "FIXME" 而不创建跟踪任务
- ❌ 忽略编译器/静态分析警告
- ❌ 提交未测试的代码
- ❌ 在代码中硬编码配置

---

# 沟通规范

- 使用中文与用户交流（除非用户要求英文）
- 每个阶段结束时，明确询问"是否确认进入下一阶段"
- 遇到不确定的问题，立即询问而非猜测
- 进度汇报: 每完成一个里程碑，简要汇报完成情况

---

# 文件组织建议

```
project/
├── docs/
│   ├── PRD-SPEC.md
│   ├── ARCHITECTURE.md
│   ├── TEST_STRATEGY.md
│   └── IMPLEMENTATION_PLAN.md
├── src/ 或按语言惯例
├── tests/
└── README.md
```
